name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: Typecheck · Lint · Test
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      NODE_OPTIONS: --max-old-space-size=4096

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Lint
        run: npm run lint

      - name: Supabase migration baseline check
        run: npm run check:supabase-migrations

      - name: Tests
        run: npm test

  migration-repair-contract:
    name: Supabase migration repair contract
    runs-on: ubuntu-latest
    timeout-minutes: 8
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10

    steps:
      - name: Setup Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: 2.117.0

      - name: Verify migration repair and rollback semantics
        shell: bash
        env:
          DB_URL: postgresql://postgres:postgres@127.0.0.1:5432/postgres?sslmode=disable
        run: |
          set -euo pipefail
          supabase --version
          workdir="$RUNNER_TEMP/repair-probe"
          mkdir -p "$workdir/supabase/migrations"
          printf 'project_id = "repair-probe"\n' > "$workdir/supabase/config.toml"
          cat > "$workdir/supabase/migrations/20990101000000_repair_probe_contract.sql" <<'SQL'
          create table public.repair_probe_contract (
            id bigint primary key
          );
          SQL

          supabase --workdir "$workdir" migration repair 20990101000000 \
            --db-url "$DB_URL" \
            --status applied

          test "$(psql "$DB_URL" -Atc "select count(*) from supabase_migrations.schema_migrations where version='20990101000000'")" = "1"
          test "$(psql "$DB_URL" -Atc "select to_regclass('public.repair_probe_contract') is null")" = "t"

          statement_count="$(psql "$DB_URL" -Atc "select coalesce(array_length(statements,1),0) from supabase_migrations.schema_migrations where version='20990101000000'")"
          statement_hash="$(psql "$DB_URL" -Atc "select md5(coalesce(array_to_string(statements,E'\\n'),'')) from supabase_migrations.schema_migrations where version='20990101000000'")"
          echo "repair-applied statement_count=$statement_count"
          test "$statement_count" -gt 0
          test -n "$statement_hash"

          backup="$workdir/schema_migrations_before.sql"
          restore="$workdir/schema_migrations_before_pg15.sql"
          supabase db dump \
            --db-url "$DB_URL" \
            --schema supabase_migrations \
            --data-only \
            --use-copy \
            --file "$backup"
          test -s "$backup"
          sha256sum "$backup" > "$backup.sha256"

          # Supabase CLI 2.117.0 currently dumps with PostgreSQL 17 tools, which emit
          # SET transaction_timeout = 0;. Production is PostgreSQL 15.8, where that
          # GUC does not exist. Preserve the raw dump unchanged, and derive a PG15
          # restore copy by removing only that compatibility SET line.
          grep -q '^SET transaction_timeout = 0;$' "$backup"
          sed '/^SET transaction_timeout = 0;$/d' "$backup" > "$restore"
          test -s "$restore"

          supabase --workdir "$workdir" migration repair 20990101000000 \
            --db-url "$DB_URL" \
            --status reverted
          test "$(psql "$DB_URL" -Atc "select count(*) from supabase_migrations.schema_migrations where version='20990101000000'")" = "0"

          psql "$DB_URL" -v ON_ERROR_STOP=1 -f "$restore" >/dev/null
          test "$(psql "$DB_URL" -Atc "select count(*) from supabase_migrations.schema_migrations where version='20990101000000'")" = "1"
          restored_hash="$(psql "$DB_URL" -Atc "select md5(coalesce(array_to_string(statements,E'\\n'),'')) from supabase_migrations.schema_migrations where version='20990101000000'")"
          test "$restored_hash" = "$statement_hash"
          test "$(psql "$DB_URL" -Atc "select to_regclass('public.repair_probe_contract') is null")" = "t"


  ledger-preflight:
    name: Supabase ledger preflight
    if: ${{ github.event_name == 'pull_request' && github.event.pull_request.number == 194 }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
      SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
      PROJECT_REF: ybtpqscmqrrjjmuoryap
      EXPECTED_LEDGER_ROWS: "156"
      EXPECTED_FIRST_VERSION: "20260508082323"
      EXPECTED_LAST_VERSION: "20260918114535"

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify required secrets
        shell: bash
        run: |
          set -euo pipefail
          test -n "${SUPABASE_ACCESS_TOKEN:-}" || { echo 'STOP: SUPABASE_ACCESS_TOKEN secret is not configured'; exit 1; }
          test -n "${SUPABASE_DB_PASSWORD:-}" || { echo 'STOP: SUPABASE_DB_PASSWORD secret is not configured'; exit 1; }

      - name: Verify recovery migration layout
        shell: bash
        run: |
          set -euo pipefail
          mapfile -t files < <(find supabase/migrations -maxdepth 1 -type f -name '*.sql' -printf '%f\n' | sort)
          test "${#files[@]}" -ge 36
          test "${files[0]}" = '20260912000100_baseline_public_primitives.sql'
          test "${files[35]}" = '20260912003600_baseline_public_acl.sql'
          for file in "${files[@]:36}"; do
            version="${file:0:14}"
            test "$version" > "20260912003600" || { echo "STOP: invalid post-baseline migration $file"; exit 1; }
          done

      - name: Setup Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: 2.117.0

      - name: Link production and capture migration list
        shell: bash
        run: |
          set -euo pipefail
          supabase link --project-ref "$PROJECT_REF" -p "$SUPABASE_DB_PASSWORD"

          migration_list_ok=0
          for attempt in 1 2 3 4 5; do
            if supabase migration list --linked > "$RUNNER_TEMP/migration-list-before.txt"; then
              migration_list_ok=1
              break
            fi
            if [ "$attempt" -lt 5 ]; then
              echo "Supavisor connection attempt $attempt failed; retrying after credential-cache window"
              sleep 20
            fi
          done
          test "$migration_list_ok" -eq 1 || {
            echo 'STOP: unable to read linked migration list after Supavisor retries'
            exit 1
          }

      - name: Capture encrypted ledger backup and validate frozen state
        shell: bash
        run: |
          set -euo pipefail
          private="$RUNNER_TEMP/ledger-preflight"
          mkdir -p "$private"
          chmod 700 "$private"
          raw="$private/schema_migrations_before_repair.raw.sql"
          restore="$private/schema_migrations_before_repair.pg15.sql"

          supabase db dump \
            --linked \
            --schema supabase_migrations \
            --data-only \
            --use-copy \
            --file "$raw"
          test -s "$raw"

          mapfile -t versions < <(
            awk 'BEGIN { FS="\t" }
              /^COPY .*schema_migrations.*FROM stdin;$/ { inside=1; next }
              inside && $0 == "\\." { inside=0; next }
              inside { print $1 }
            ' "$raw" | sort
          )

          test "${#versions[@]}" -eq "$EXPECTED_LEDGER_ROWS" || {
            echo "STOP: expected $EXPECTED_LEDGER_ROWS ledger rows, got ${#versions[@]}"
            exit 1
          }
          test "${versions[0]}" = "$EXPECTED_FIRST_VERSION" || { echo 'STOP: unexpected first ledger version'; exit 1; }
          last_index=$((EXPECTED_LEDGER_ROWS - 1))
          test "${versions[$last_index]}" = "$EXPECTED_LAST_VERSION" || { echo 'STOP: unexpected production migration tip'; exit 1; }

          if printf '%s\n' "${versions[@]}" | grep -q '^2026091200'; then
            echo 'STOP: recovery baseline tracking versions already exist remotely'
            exit 1
          fi

          empty_statements="$(awk 'BEGIN { FS="\t" }
            /^COPY .*schema_migrations.*FROM stdin;$/ { inside=1; next }
            inside && $0 == "\\." { inside=0; next }
            inside && ($2 == "\\N" || $2 == "{}" || $2 == "") { empty++ }
            END { print empty+0 }
          ' "$raw")"
          test "$empty_statements" -eq 0 || { echo "STOP: $empty_statements ledger rows have empty statements"; exit 1; }

          sha256sum "$raw" > "$private/schema_migrations_before_repair.raw.sha256"

          if grep -q '^SET transaction_timeout = 0;$' "$raw"; then
            sed '/^SET transaction_timeout = 0;$/d' "$raw" > "$restore"
          else
            cp "$raw" "$restore"
          fi
          test -s "$restore"
          sha256sum "$restore" > "$private/schema_migrations_before_repair.pg15.sha256"

          cp "$RUNNER_TEMP/migration-list-before.txt" "$private/migration-list-before.txt"

          openssl enc -aes-256-cbc -salt -pbkdf2 -iter 200000 \
            -pass env:SUPABASE_DB_PASSWORD \
            -in "$raw" \
            -out "$private/schema_migrations_before_repair.raw.sql.enc"
          openssl enc -aes-256-cbc -salt -pbkdf2 -iter 200000 \
            -pass env:SUPABASE_DB_PASSWORD \
            -in "$restore" \
            -out "$private/schema_migrations_before_repair.pg15.sql.enc"

          rm -f "$raw" "$restore"
          chmod 600 "$private"/*

      - name: Upload encrypted ledger backup
        uses: actions/upload-artifact@v4
        with:
          name: supabase-ledger-preflight-20260918
          path: ${{ runner.temp }}/ledger-preflight/
          if-no-files-found: error
          retention-days: 14


  ledger-repair-execution:
    name: Supabase ledger repair execution
    if: ${{ github.event_name == 'pull_request' && github.head_ref == 'infra/supabase-ledger-repair-execution' }}
    runs-on: ubuntu-latest
    timeout-minutes: 25
    env:
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
      SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
      PROJECT_REF: ybtpqscmqrrjjmuoryap
      EXPECTED_ROWS_BEFORE: "156"
      EXPECTED_ROWS_AFTER: "59"
      EXPECTED_FIRST_BEFORE: "20260508082323"
      EXPECTED_LAST: "20260918114535"

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify required secrets
        shell: bash
        run: |
          set -euo pipefail
          test -n "${SUPABASE_ACCESS_TOKEN:-}" || { echo 'STOP: SUPABASE_ACCESS_TOKEN secret is not configured'; exit 1; }
          test -n "${SUPABASE_DB_PASSWORD:-}" || { echo 'STOP: SUPABASE_DB_PASSWORD secret is not configured'; exit 1; }

      - name: Setup Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: 2.117.0

      - name: Link production
        shell: bash
        run: |
          set -euo pipefail
          supabase link --project-ref "$PROJECT_REF" -p "$SUPABASE_DB_PASSWORD"

      - name: Freeze before-state and encrypted rollback backup
        shell: bash
        run: |
          set -euo pipefail
          private="$RUNNER_TEMP/ledger-repair"
          mkdir -p "$private"
          chmod 700 "$private"
          raw="$private/schema_migrations_before.raw.sql"
          restore="$private/schema_migrations_before.pg15.sql"

          supabase migration list --linked > "$private/migration-list-before.txt"

          supabase db dump             --linked             --schema supabase_migrations             --data-only             --use-copy             --file "$raw"
          test -s "$raw"

          mapfile -t versions < <(
            awk 'BEGIN { FS="\t" }
              /^COPY .*schema_migrations.*FROM stdin;$/ { inside=1; next }
              inside && $0 == "\\." { inside=0; next }
              inside { print $1 }
            ' "$raw" | sort
          )

          test "${#versions[@]}" -eq "$EXPECTED_ROWS_BEFORE" || {
            echo "STOP: expected $EXPECTED_ROWS_BEFORE ledger rows, got ${#versions[@]}"
            exit 1
          }
          test "${versions[0]}" = "$EXPECTED_FIRST_BEFORE" || { echo 'STOP: unexpected first ledger version'; exit 1; }
          last_index=$((EXPECTED_ROWS_BEFORE - 1))
          test "${versions[$last_index]}" = "$EXPECTED_LAST" || { echo 'STOP: unexpected ledger tip'; exit 1; }

          prebaseline_count="$(printf '%s\n' "${versions[@]}" | awk '$1 <= "20260911174615" {c++} END {print c+0}')"
          postbaseline_count="$(printf '%s\n' "${versions[@]}" | awk '$1 > "20260911174615" {c++} END {print c+0}')"
          baseline_count="$(printf '%s\n' "${versions[@]}" | grep -c '^2026091200' || true)"
          test "$prebaseline_count" -eq 133 || { echo "STOP: expected 133 pre-baseline rows, got $prebaseline_count"; exit 1; }
          test "$postbaseline_count" -eq 23 || { echo "STOP: expected 23 post-baseline rows, got $postbaseline_count"; exit 1; }
          test "$baseline_count" -eq 0 || { echo "STOP: baseline versions already tracked remotely"; exit 1; }

          empty_statements="$(awk 'BEGIN { FS="\t" }
            /^COPY .*schema_migrations.*FROM stdin;$/ { inside=1; next }
            inside && $0 == "\\." { inside=0; next }
            inside && ($2 == "\\N" || $2 == "{}" || $2 == "") { empty++ }
            END { print empty+0 }
          ' "$raw")"
          test "$empty_statements" -eq 0 || { echo "STOP: $empty_statements rows have empty statements"; exit 1; }

          sha256sum "$raw" > "$private/schema_migrations_before.raw.sha256"
          if grep -q '^SET transaction_timeout = 0;$' "$raw"; then
            sed '/^SET transaction_timeout = 0;$/d' "$raw" > "$restore"
          else
            cp "$raw" "$restore"
          fi
          test -s "$restore"
          sha256sum "$restore" > "$private/schema_migrations_before.pg15.sha256"

          openssl enc -aes-256-cbc -salt -pbkdf2 -iter 200000             -pass env:SUPABASE_DB_PASSWORD             -in "$raw"             -out "$private/schema_migrations_before.raw.sql.enc"
          openssl enc -aes-256-cbc -salt -pbkdf2 -iter 200000             -pass env:SUPABASE_DB_PASSWORD             -in "$restore"             -out "$private/schema_migrations_before.pg15.sql.enc"

          rm -f "$raw" "$restore"
          chmod 600 "$private"/*

      - name: Capture application schema before
        shell: bash
        run: |
          set -euo pipefail
          private="$RUNNER_TEMP/ledger-repair"
          supabase db dump --linked --schema public,app,private --file "$private/application-schema-before.sql"
          sha256sum "$private/application-schema-before.sql" | tee "$private/application-schema-before.sha256"

      - name: Reconcile migration history only
        shell: bash
        run: |
          set -euo pipefail

          mapfile -t OLD_VERSIONS < <(
            awk '
              /^OLD_VERSIONS=\(/ { inside=1; next }
              inside && /^\)/ { exit }
              inside {
                for (i=1; i<=NF; i++) print $i
              }
            ' scripts/supabase-ledger-repair-20260918.sh
          )
          mapfile -t BASELINE_VERSIONS < <(
            awk '
              /^BASELINE_VERSIONS=\(/ { inside=1; next }
              inside && /^\)/ { exit }
              inside {
                for (i=1; i<=NF; i++) print $i
              }
            ' scripts/supabase-ledger-repair-20260918.sh
          )

          test "${#OLD_VERSIONS[@]}" -eq 133
          test "${#BASELINE_VERSIONS[@]}" -eq 36
          test "${OLD_VERSIONS[0]}" = "20260508082323"
          test "${OLD_VERSIONS[132]}" = "20260911174615"
          test "${BASELINE_VERSIONS[0]}" = "20260912000100"
          test "${BASELINE_VERSIONS[35]}" = "20260912003600"

          supabase migration repair "${OLD_VERSIONS[@]}" --linked --status reverted
          supabase migration repair "${BASELINE_VERSIONS[@]}" --linked --status applied

      - name: Verify repaired ledger and dry-run
        shell: bash
        run: |
          set -euo pipefail
          private="$RUNNER_TEMP/ledger-repair"

          supabase migration list --linked | tee "$private/migration-list-after.txt"
          supabase db dump             --linked             --schema supabase_migrations             --data-only             --use-copy             --file "$private/schema_migrations_after.sql"

          mapfile -t versions < <(
            awk 'BEGIN { FS="\t" }
              /^COPY .*schema_migrations.*FROM stdin;$/ { inside=1; next }
              inside && $0 == "\\." { inside=0; next }
              inside { print $1 }
            ' "$private/schema_migrations_after.sql" | sort
          )
          test "${#versions[@]}" -eq "$EXPECTED_ROWS_AFTER" || {
            echo "STOP: expected $EXPECTED_ROWS_AFTER rows after repair, got ${#versions[@]}"
            exit 1
          }

          baseline_count="$(printf '%s\n' "${versions[@]}" | grep -c '^2026091200' || true)"
          legacy_count="$(printf '%s\n' "${versions[@]}" | awk '$1 <= "20260911174615" {c++} END {print c+0}')"
          post_count="$(printf '%s\n' "${versions[@]}" | awk '$1 > "20260912003600" {c++} END {print c+0}')"
          test "$baseline_count" -eq 36 || { echo "STOP: expected 36 baseline versions after repair"; exit 1; }
          test "$legacy_count" -eq 0 || { echo "STOP: legacy versions remain after repair"; exit 1; }
          test "$post_count" -eq 23 || { echo "STOP: expected 23 preserved post-baseline versions, got $post_count"; exit 1; }
          test "${versions[58]}" = "$EXPECTED_LAST" || { echo "STOP: unexpected post-repair tip"; exit 1; }

          supabase db push --linked --dry-run 2>&1 | tee "$private/db-push-dry-run.txt"

      - name: Verify application schema unchanged
        shell: bash
        run: |
          set -euo pipefail
          private="$RUNNER_TEMP/ledger-repair"
          supabase db dump --linked --schema public,app,private --file "$private/application-schema-after.sql"
          sha256sum "$private/application-schema-after.sql" | tee "$private/application-schema-after.sha256"
          cmp -s "$private/application-schema-before.sql" "$private/application-schema-after.sql" || {
            echo 'STOP: application schema changed during history-only repair'
            diff -u "$private/application-schema-before.sql" "$private/application-schema-after.sql" | head -200 || true
            exit 1
          }

      - name: Upload repair evidence and encrypted rollback backup
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: supabase-ledger-repair-execution-20260918
          path: ${{ runner.temp }}/ledger-repair/
          if-no-files-found: error
          retention-days: 14
